# my-1st-love

A personal workspace for agentic coding experiments — skills discovery, indexing, and project scaffolding.

## What's in this repo

| File | Purpose |
| :--- | :--- |
| [`projectskillindex.md`](./projectskillindex.md) | 📇 Indexed directory of all globally installed agent skills (89 skills at `C:\Users\hp\.agents\skills\`) — game dev engines, HTML artifact/presentation skills, and meta-routing. |
| `.gitignore` | General-purpose ignore rules — OS files, editor cruft, secrets, logs, common build output. |
| `.editorconfig` | Consistent charset, line endings, and indentation across editors. |
| `.gitattributes` | Line-ending normalization and binary asset marking for Git. |

## Skills quick facts

- **Global library:** `~/.agents/skills/` → 89 skills (Godot, Unity, Unreal, Bevy, Phaser, PixiJS, three.js, Roblox, cross-engine systems, HTML artifacts)
- **Claude skills:** `~/.claude/skills/` → empty
- **Project-local:** none installed yet — add with `npx skills add <owner/repo> --skill <name> --yes`
- **Meta-skill:** ask the agent to use `router` to pick the right engine skill, or `find-skills` to hunt for new ones

See **[projectskillindex.md](./projectskillindex.md)** for the full indexed directory with paths and descriptions.

## Setup

No code yet — this repo currently holds configuration and documentation only. Clone and go:

```bash
git clone <repo-url>
cd my-1st-love
```
